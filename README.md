# PromptPay Admin (fe-admin)

Payments list — shows matched vs unmatched state so operators can
reconcile manually when the bank strips the reference.

Pairs with [`vbwd-plugin-promptpay`](https://github.com/VBWD-platform/vbwd-plugin-promptpay).
